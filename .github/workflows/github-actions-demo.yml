name: Aha-assignment-timpei
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
  push
jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      - name: get timzone
        run: date