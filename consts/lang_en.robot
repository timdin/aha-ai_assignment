# since the test cases uses a lot of locators based on texts, refactor and add a variable file to better manage 
# these hard-coded values
*** Variables ***
${LOGIN} =    "Login"
${CREATE CLUB}=    "Create Club"
${LOGOUT} =    "Log out"
${LOGOUT YES} =    "Log Out"
${EDIT PROFILE} =    "Edit Profile"
${OK} =    "OK"
${SAVE} =    "Save"